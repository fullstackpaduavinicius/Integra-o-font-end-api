import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './main.jsx'; // Certifique-se de que este caminho está correto

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
